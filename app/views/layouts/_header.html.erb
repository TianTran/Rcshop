	<!--menu-top-->
	<script>
		$(function () {
			$('[data-toggle="tooltip"]').tooltip()
		})
	</script>
		<div class="menu-top">
			<div class="container">
				<div class="pull-left">
					<ul class="nav nav-tabs">
					  <li role="presentation" class="active" data-toggle="tooltip" data-placement="bottom" title="home"><%=link_to "Home",root_path%></li>
					  <li role="presentation"><a data-toggle="tooltip" data-placement="bottom" title="contact" href="#">Contact</a></li>
					  <li role="presentation"><a data-toggle="tooltip" data-placement="bottom" title="sitemap" href="#">Sitemap</a></li>
					</ul>
				</div>
				<div class="pull-right">
					<ul class="nav nav-tabs">
						<% if current_user %>
							<li><%= link_to current_user.email %></li>
							<li><%= link_to "Sign Out",destroy_user_session_path, data:{confirm: "Are you sure?"}, method: :delete %></li>
						<% else %>
							<li><%= link_to "Log In",new_user_session_path %></li>
							<li><%= link_to "Sign Up",new_user_registration_path %></li>
						<% end %>
						<li>
							<a href="#">
								En <span class="glyphicon glyphicon-ok-sign" aria-hidden="true"></span>
							</a>
						</li>
						<li>
							<a href="#">
								$ <span class="glyphicon glyphicon-ok-sign" aria-hidden="true"></span>
							</a>
						</li>
					</ul>
				</div>
			</div>
		</div>
	<!--end-menu-top-->
	<!--banner-header-->
		<div class="banner-top">
			<div class="container">
				<div class="row">
					<div class="col-md-6">
					<img class="pull-left" src="http://livedemo00.template-help.com/prestashop_52530/img/new-store-1416400908.jpg"/>
				</div>
				<div class="col-md-4">
					<p class="text-capitalize">CALL US: (800) 2345-6789</p>
				</div>
				<div class="col-md-2">
					<h1 class="cart-text"><%= render 'layouts/cart_text' %></h1>
				</div>
				</div>
			</div>
		</div>
	<!-- end -->
	<!--main-menu-->
	<script>
	   $(document).ready(function(){
		   $(window).bind('scroll', function() {
		   var navHeight = $('.banner-top').height();
				 if ($(window).scrollTop() > navHeight) {
					 $('#test').addClass('navbar-fixed-top');
				 }
				 else {
					 $('#test').removeClass('navbar-fixed-top');
				 }
			});
		});
	</script>
		<div class="main-menu" id="test">
			<div class="container">
				<div class="row">
					<div class="col-md-8">
						<nav class="navbar navbar-default">
							<div class="navbar-header">
								<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
									<span class="sr-only">Toggle navigation</span>
									<span>CATEGORIES</span>
								</button>
							</div>
							<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
								<ul class="nav navbar-nav cl-effect">
								<%- model_class = Category -%>
									<% @categories.each do |category| %>
										<li><%= link_to category.name, category_path(category), class: "text-uppercase" %></li>
									<% end %>
								</ul>
							</div>
						</nav>
					</div>
					<div class="col-md-4">
						<%= form_tag(search_path, :method => "get", class: "navbar-form navbar-right") do %>
								<div class="form-group">
									<%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Search Name" %>
								</div>
								<button type="submit" class="btn btn-default">Search</button>
						<% end %>
					</div>
				</div>
			
			</div>
		</div>
	<!--end-->